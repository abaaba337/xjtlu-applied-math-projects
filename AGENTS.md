# 项目维护规则

本项目包含四个独立的本科科研与课程项目。先读根 README 与目标子目录 README，确认研究问题、实际运行入口、输入数据、结果来源和已知缺口；COVID 子目录另有适用规则。

- 默认启用 `ponytail full`。修改前依次检查需求是否必要、仓内复用、标准库、原生能力、已有依赖、一行实现和最小实现；不要引入统一训练框架、无调用方配置或预留接口。
- 修改共享函数前搜索全部调用方。CMIT 的 DRIVE 加载共用 `cmit_utils.py`；COVID 共用 `analysis_utils.py` 和 `project_paths.py`；竞赛算法的命令行入口与测试直接使用同一函数。
- 不把测试数据、合成演示或后续复现代码标成历史实验结果。报告中的贪心解不构成全局最优性证明；统计关联不构成因果结论。
- 保留时间划分、训练集拟合缩放器、掩码最近邻插值、矩阵约束、日期/FIPS/连接基数校验及缺失值语义。不得用随机模型或零值替代缺失数据，声称完成复现。
- 保留报告和课程资源、作者信息、第三方引用及已有模型。新输出不得覆盖随附模型；数据路径使用项目相对位置或说明中的环境变量。
- 不提交运行输出、缓存、虚拟环境、凭据或新增大数据；笔记本提交前清除输出和执行计数。既有随附材料可保留，不擅自清理历史资源。
- 非平凡代码改动保留能检出回归的最小检查。优先复用当前 `unittest` 或 COVID 的 `pytest`；在对应项目目录运行 README 中的检查，并执行 `git diff --check`。
- 每次代码改动完成前按 `ponytail-review` 审查当前 diff；完整说明已运行的检查、尚未运行的真实数据流程和缺失输入。不得擅自改写报告、科研参数或宣称未验证的性能提升。

`CLAUDE.md` 只引用本文件，避免维护两套规则。
